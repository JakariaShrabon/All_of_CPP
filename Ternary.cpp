// why we want to use ternary operator in cpp language
// we are usally use if else operator
// but if  we use ternary operator we can use if else in short write of code

#include <iostream>
using namespace std;
int main()
{
    int marks = 45;
    marks >= 40 ? cout << "You are pass" : cout << "You are fail";
    system("pause>0");
}